# --- REQUIREMENTS FOR SELF-OPTIMIZING LIVE OPS ---
# All dependencies required for continuous optimization and perfect live ops
Flask==2.3.3
Flask-CORS==4.0.0
requests==2.33.0
PyJWT==2.12.0
python-dotenv==1.0.0
Flask-Limiter==3.4.0
# For automated container management
# subprocess/threading are standard library
# For production: gunicorn, eventlet, or uvicorn (optional)
# gunicorn==21.2.0
# eventlet==0.35.0
# uvicorn==0.23.2
# --- END REQUIREMENTS ---
