#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'EOF'
REFUSED: NON-COMMANDING public proof compatibility entrypoint.

This public repository is an artifact-only proof lane. Phi/Dominion orchestration,
autonomous maintenance, destructive cleanup, private runtime control, and
operator authority belong in governed private systems and are intentionally
unavailable here.

No process, file, repository, runtime, or cloud resource was modified.
EOF

exit 2
