# PR #17 Resolve Threads

Threads to resolve:

- Shader constant correction (now using `gl.COMPILE_STATUS`)
- Program link status validation (`gl.LINK_STATUS`)
- Input sanitization and duplicate fetch removal in terrain viewer
- VS Code settings modernization

Mark each as resolved in the PR UI.
