name: Public Proof - Production Deploy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  boundary:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse public deployment authority
        run: |
          echo 'PUBLIC PROOF LANE: production deployment is disabled in this repository.'
          echo 'Phi is the sole Dominion command service. Use the private Phi-governed deployment lane.'
