name: governance-suite

on:
    pull_request:
        branches:
            - main

jobs:
    governance:
        runs-on: ubuntu-latest
        steps:
            - name: Run governance suite stub
              run: echo "governance suite ok"
