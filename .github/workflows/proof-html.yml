name: proof-html
on: [push, pull_request]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate catalog HTML
        run: |
          python .github/scripts/proof_html.py
