name: Public Proof - Production Deployment Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  boundary:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse public production deployment
        run: |
          echo 'PUBLIC PROOF LANE: production deployment is disabled in this repository.'
          echo 'Build, release, and deployment authority must remain in private Phi-governed infrastructure with receipts.'
