# Public Environment Handling

This public repository is for demo-build artifacts only.

Local runtime configuration belongs outside the repository. Developers should keep machine-specific configuration untracked and use approved internal stores for operational values.

The root environment file has been removed from this hardening branch. The repository ignore rules already exclude local environment files.
