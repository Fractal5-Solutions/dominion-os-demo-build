name: demo-success-gate
on: [push, pull_request]
jobs:
    run:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Catalog success snapshot
              run: |
                  python .github/scripts/demo_success_gate.py
